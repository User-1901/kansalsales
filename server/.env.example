# Server
PORT=3000

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/kansal_sales

# JWT
JWT_SECRET=change-me-to-a-long-random-secret

# Email (SMTP)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password

# Admin contact email (receives contact form submissions)
ADMIN_EMAIL=admin@kansalsales.com
